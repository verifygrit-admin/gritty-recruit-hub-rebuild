// CreateRowModal — Sprint 027 stub.
// ONLY imported by CollegesView (no-op — schools_deny_insert blocks),
// CollegeCoachesView, RecruitingEventsView. Per Q5 LOCKED, NOT imported by
// the 4 auth-linked views (compile-time gate).
//
// Props (planned): entity, onClose, onSubmit. Required-field validation
// reads create_required from entityRegistry.
//
// PHASE 1 STUB — no implementation.

export default function CreateRowModal() {
  throw new Error('CreateRowModal: Phase 1 stub. Implement in Phase 2 task 2.C3.');
}
